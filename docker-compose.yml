services:
  mongo:
    image: mongo
    container_name: "swapi-mern-example-${NODE_ENV}-mongo"
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_INITDB_ROOT_USERNAME}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_INITDB_ROOT_PASSWORD}
    ports:
      - "${MONGO_PORT}:27017"
    volumes:
      - mongo-data:/data/db
    command: mongod --quiet
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh --quiet
      interval: 3s
      timeout: 5s
      retries: 5
      start_period: 5s

  api:
    container_name: "swapi-mern-example-${NODE_ENV}-api"
    build:
      context: .
      dockerfile: packages/api/Dockerfile.development
    ports:
      - "${API_PORT}:5000"
    volumes:
      - .:/app
      - /app/build
      - /app/node_modules
      - /app/packages/api/build
      - /app/packages/api/node_modules
      - /app/packages/domain/build
      - /app/packages/domain/dist
      - /app/packages/domain/node_modules
    env_file:
      - packages/api/.env
    depends_on:
      mongo:
        condition: service_healthy
    command: pnpm --filter api dev
    healthcheck:
      test: ["CMD", "curl", "-f", "${VITE_SEARCHES_STATS_API_URL}/health"]
      interval: 3s
      timeout: 10s
      retries: 5
      start_period: 10s

  pwa:
    container_name: "swapi-mern-example-${NODE_ENV}-pwa"
    build:
      context: .
      dockerfile: packages/pwa/Dockerfile.development
      args:
        - VITE_PWA_PORT=${VITE_PWA_PORT}
        - VITE_SEARCHES_STATS_API_URL=${VITE_SEARCHES_STATS_API_URL}
        - VITE_SWAPI_API_URL=${VITE_SWAPI_API_URL}
    ports:
      - "${VITE_PWA_PORT}:${PWA_CONTAINER_PORT}"
    volumes:
      - .:/app
      - /app/build
      - /app/node_modules
      - /app/packages/pwa/build
      - /app/packages/pwa/node_modules
      - /app/packages/domain/build
      - /app/packages/domain/dist
      - /app/packages/domain/node_modules
    env_file:
      - packages/pwa/.env
    depends_on:
      api:
        condition: service_healthy
    command: pnpm --filter pwa dev
    healthcheck:
      test: ["CMD", "curl", "-f", "${PWA_URL}"]
      interval: 3s
      timeout: 10s
      retries: 5
      start_period: 10s

volumes:
  mongo-data:
    name: "swapi-mern-example-${NODE_ENV}-mongo-data"
