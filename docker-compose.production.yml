services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile.production
    image: mern-swapi1vite-mern-template-backend-prod:latest
    volumes: []

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile.production
    image: mern-swapi1vite-mern-template-frontend-prod:latest
    volumes: []
